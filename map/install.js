// 导入类
// const files = require.context('./modules', true, /index.js/)
// const md = {}
// files.keys().forEach((key) => {
//     const fileName = files(key).default.name
//     md[fileName] = files(key).default
// })
// module.exports = md;